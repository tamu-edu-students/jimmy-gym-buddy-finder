<!DOCTYPE html>
<html>
<head>
  <title><%= content_for(:title) || "Jimmy Gym Buddy Finder" %></title>
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= yield :head %>
</head>

<body>
  <nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">
      <%= link_to "Jimmy", root_path, class: 'navbar-brand main-heading' %>
      
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation" data-turbo="false">
        <span class="navbar-toggler-icon"></span>
      </button>
      
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ms-auto">
          <% if session[:user_id] %>
            <li class="nav-item">
              <%= link_to "Dashboard", dashboard_user_path, class: "nav-link" %>
            </li>
            <li class="nav-item">
              <%= link_to "Profile", profile_user_path(@user), class: "nav-link" %>
            </li>
            <li class="nav-item">
              <%= link_to "Log Out", logout_path, method: :delete, class: "nav-link btn btn-danger text-white" %>
            </li>
          <% end %>
        </ul>
      </div>
    </div>
  </nav>

  <div class="container mt-4">
    <%= render partial: 'shared/display_flash' if flash.any? %>
  </div>

  <div class="container-fluid mt-4">
    <%= yield %>
  </div>
  
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  <%= javascript_include_tag "application", "data-turbo-track": "reload" %>

</body>
</html>
