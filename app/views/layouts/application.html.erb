<!DOCTYPE html>
<html>
<head>
  <title><%= content_for(:title) || "Jimmy Gym Buddy Finder" %></title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= yield :head %>
</head>

<body>
  <nav class="navbar">
    <div class="container">
      <%= link_to "Jimmy", root_path, class: 'main-heading'%>
      <div class="nav-links">
        <%= link_to "Dashboard", dashboard_user_path, class: "btn" if session[:user_id] %>
        <%= link_to "Profile", profile_user_path(@user), class: "btn" if session[:user_id] %>
        <%= link_to "Log Out", logout_path, class: "btn btn-danger" if session[:user_id] %>
      </div>
    </div>
  </nav>

  <%= render partial: 'shared/display_flash' if flash.any? %>

  <div class="container">
    <%= yield %>
  </div>
</body>
</html>
